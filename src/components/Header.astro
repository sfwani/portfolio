<!-- Empty header component --> 

<nav class="main-nav">
  <div class="nav-container">
    <div class="nav-brand">
      <a href="/" class="brand-link-subtle">$sfwani</a>
    </div>
    <div class="nav-links">
      <a href="/blog" class="nav-link"><span class="prefix">./</span><span class="link-text">blog</span></a>
      <a href="/competitions" class="nav-link"><span class="prefix">./</span><span class="link-text">competitions</span></a>
      <a href="/projects" class="nav-link"><span class="prefix">./</span><span class="link-text">projects</span></a>
      <a href="/resume" class="nav-link"><span class="prefix">./</span><span class="link-text">resume.txt</span></a>
    </div>
  </div>
</nav>

<script>
  // Add active class to current page nav link
  document.addEventListener('DOMContentLoaded', () => {
    const currentPath = window.location.pathname;
    const navLinks = document.querySelectorAll('.nav-link');
    
    navLinks.forEach(link => {
      const href = link.getAttribute('href');
      if (href === currentPath || (currentPath.startsWith(href) && href !== '/')) {
        link.classList.add('active');
      }
    });
  });
</script>

<style>
  :root {
    --accent-blue: #0070f3;
  }

  .main-nav {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    padding: 1.8rem 2rem;
    z-index: 10;
    backdrop-filter: blur(5px);
    background: rgba(0, 0, 0, 0.2);
    border-bottom: 1px solid rgba(51, 51, 51, 0.2);
  }
  
  .nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: var(--max-width);
    margin: 0 auto;
  }
  
  .nav-brand {
    font-size: 1.2rem;
    font-weight: 500;
  }
  
  .brand-link-subtle {
    text-decoration: none;
    font-family: var(--font-mono);
    letter-spacing: -0.5px;
    transition: all 0.2s ease;
    color: var(--text-main);
    position: relative;
    opacity: 0.9;
  }
  
  .brand-link-subtle:hover {
    opacity: 1;
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.2);
  }
  
  .nav-links {
    display: flex;
    gap: 2.2rem;
    font-family: var(--font-mono);
    font-size: 1.15rem;
  }
  
  .nav-link {
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
    position: relative;
    padding: 0.45rem 0;
    opacity: 0.85;
  }
  
  .nav-link .prefix {
    color: var(--text-main);
    opacity: 0.7;
    font-weight: 500;
  }
  
  .nav-link .link-text {
    color: var(--text-main);
    letter-spacing: 0.01em;
  }
  
  .nav-link:hover, 
  .nav-link.active {
    opacity: 1;
    transform: translateY(-2px);
  }
  
  .nav-link:hover .prefix,
  .nav-link:hover .link-text,
  .nav-link.active .prefix,
  .nav-link.active .link-text {
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.2);
  }
  
  .nav-link.active .prefix,
  .nav-link.active .link-text {
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.3);
    color: rgba(255, 255, 255, 0.95);
  }
  
  .nav-link::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 1px;
    background: linear-gradient(to right, var(--text-main) 10%, transparent);
    transition: width 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
    opacity: 0.2;
  }
  
  .nav-link:hover::after {
    width: 100%;
    opacity: 0.4;
  }
  
  .nav-link.active::after {
    width: 100%;
    opacity: 0.7;
    background: linear-gradient(to right, rgba(255, 255, 255, 0.9) 30%, rgba(255, 255, 255, 0.3));
    height: 2px;
    animation: glow 1.5s ease-in-out infinite alternate;
  }
  
  .nav-link.active::before {
    display: none;
  }
  
  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 rgba(255, 215, 0, 0.4);
    }
    70% {
      box-shadow: 0 0 0 8px rgba(255, 215, 0, 0);
    }
    100% {
      box-shadow: 0 0 0 0 rgba(255, 215, 0, 0);
    }
  }
  
  @keyframes glow {
    from {
      box-shadow: 0 0 2px rgba(255, 255, 255, 0.2);
    }
    to {
      box-shadow: 0 0 6px rgba(255, 255, 255, 0.5);
    }
  }
  
  @media (max-width: 600px) {
    .main-nav {
      padding: 1.2rem 1.5rem;
    }
    
    .nav-container {
      flex-direction: column;
      gap: 1rem;
    }
    
    .nav-links {
      gap: 1.4rem;
      font-size: 1.1rem;
    }
  }
</style> 