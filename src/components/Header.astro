<!-- Empty header component --> 

<nav class="main-nav">
  <div class="nav-container">
    <div class="nav-brand">
      <a href="/" class="brand-link">$sfwani</a>
    </div>
    <div class="nav-links">
      <a href="/blog" class="nav-link">./blog</a>
      <a href="/competitions" class="nav-link">./competitions</a>
      <a href="/projects" class="nav-link">./projects</a>
      <a href="/resume" class="nav-link">./resume.txt</a>
    </div>
  </div>
</nav>

<style>
  .main-nav {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    padding: 1.5rem;
    z-index: 10;
  }
  
  .nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: var(--max-width);
    margin: 0 auto;
  }
  
  .nav-brand {
    font-size: 1.1rem;
  }
  
  .brand-link {
    color: var(--text-main);
    text-decoration: none;
    font-family: var(--font-mono);
    letter-spacing: -0.5px;
    transition: opacity 0.2s ease;
  }
  
  .brand-link:hover {
    opacity: 0.8;
  }
  
  .nav-links {
    display: flex;
    gap: 1.5rem;
    font-family: var(--font-mono);
    font-size: 0.9rem;
  }
  
  .nav-link {
    color: var(--text-secondary);
    text-decoration: none;
    transition: all 0.2s ease;
    position: relative;
    padding-bottom: 2px;
  }
  
  .nav-link:hover {
    color: var(--text-main);
  }
  
  .nav-link:hover::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
    background-color: var(--text-secondary);
    opacity: 0.5;
  }
  
  @media (max-width: 600px) {
    .nav-container {
      flex-direction: column;
      gap: 1rem;
    }
    
    .nav-links {
      gap: 1rem;
      font-size: 0.8rem;
    }
  }
</style> 